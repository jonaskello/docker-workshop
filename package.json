{
  "name": "docker2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.7.1"
  },
  "scripts": {
    "start": "PGHOST=localhost PGPORT=3500 PGUSER=postgres PGPASSWORD=olle node index.js",
    "docker-build": "docker build -t dd:latest ."
  }
}
